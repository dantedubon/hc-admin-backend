export type City = {
  id: number,
  label: string,
};

export type Province = {
  id: number,
  label: string,
};

export type Sector = {
  id: number,
  label: string,
};

export type Command = {
  type: string,
};
